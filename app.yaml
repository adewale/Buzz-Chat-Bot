application: kiwibuzzer
version: 1
runtime: python
api_version: 1

handlers:
- url: /css
  static_dir: css
- url: /images
  static_dir: images
- url: /.*
  script: main.py

inbound_services:
- xmpp_message